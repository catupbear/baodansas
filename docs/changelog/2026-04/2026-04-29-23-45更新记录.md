# 2026-04-29 23:45 更新记录

## 需求说明
- 自动识别记录顶部筛选区添加"清空筛选"按钮，一键重置所有筛选条件
- 新增批量下载保单功能，选中多条记录后可打包为 ZIP 下载（通过 COS URL 获取文件）

## 代码变更

### 1. 清空筛选按钮
- `web/templates/insurance.html`
  - 筛选区第一行末尾添加"× 清空筛选"按钮
  - 新增 `clearAllFilters()` 函数，重置 `recordFilter` 所有字段并收起高级搜索

### 2. 批量下载保单（ZIP 打包）
- `insurance/api.py`
  - 新增 `POST /api/insurance/batch-download` 接口
  - 接收 `record_ids` 数组，逐个从 COS 下载 PDF，内存中打包为 ZIP 返回
  - 支持重名文件自动加后缀，限制单次最多 500 条
- `web/templates/insurance.html`
  - 批量操作栏新增"批量下载"按钮
  - 新增 `batchDownload()` 函数，调用后端接口并触发浏览器下载

## 数据库变更
无

## 部署步骤
1. 更新代码：`git pull`
2. 重启服务：`python main.py`
