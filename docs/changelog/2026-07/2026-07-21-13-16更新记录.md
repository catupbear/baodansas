# 2026-07-21 13:16 更新记录

## 需求说明

客户群「熠辉（陈淑娴）-TZ004-4」今日 13:07 收到的「补充信息未录入」提醒（车牌 粵F2K375）暴露 3 个问题：

1. **文案误导客户重发**：原文案写"确认保单已识别后，请重新发送一次补充信息即可"，与实际行为不符——补充信息已入待匹配池暂存，保单识别成功后系统会自动补录（还会发"补录成功"通知），无需客户重发。
2. **群消息被 @所有人**：本应只 @发送人 和 @客户客服，实际发出的消息却是"@所有人"开头。
3. **繁体车牌永远匹配不上（隐藏 bug）**：客户输入繁体"粵F2K375"入池，而 PDF 解析侧车牌为简体"粤F2K375"，`fill_pending._norm_plate` 不做繁简转换（`insurance/db.py` 的 `normalize_plate_keyword` 会转），`match_and_apply` 按车牌等值查询永远匹配不上 → 补充信息永远无法自动补录，与提醒承诺矛盾。

### @所有人 问题排查结论（非本系统代码问题）

- 系统发送 payload 落库可查（flowbot_push_log id=19，taskId=2026072113072308980bb85f3f8ddd2）：`atList: ["小玖", "客户客服（午虎保单台账系统）"]`，**未请求 @所有人**。
- 对比：7-20 09:58 同一群、同样 atList 发送成功且正确显示"@小玖@客户客服（…）"；历史上还有多条"@␣␣"开头（@ 执行丢失）的消息。
- 结论：@ 由第三方 FlowBot（飞流机器人）UI 自动化执行，@成员匹配不稳定，本次错误地执行成了 @所有人。需拿 taskId 向飞流反馈；缓解措施：微信群里只有群主/管理员能 @所有人，若机器人账号无需管理员身份，可取消其管理员，从根上杜绝误 @所有人。

## 数据库变更

无表结构变更。需修复存量数据（待匹配池中的繁体车牌，共 2 条）：

```sql
UPDATE policy_fill_pending
SET plate = REPLACE(plate, '粵', '粤')
WHERE status IN ('pending', 'notified') AND plate LIKE '%粵%';
-- 影响：id=6 粵F2K375（TZ004-4）、id=3 粵GJ87B（TZ014）
```

## 代码变更

`insurance/fill_pending.py`：

1. `MISS_TEMPLATE` 文案重写：去掉"请重新发送一次补充信息"，改为"补充信息已暂存，补发保单识别成功后系统自动录入，无需重发；车牌输错才需按正确车牌重发"。
   - `insurance_config.quote_fill_miss_notify.template` 线上为空，实际生效的就是该代码默认文案，改代码即可。
2. `_norm_plate` 改为复用 `insurance.db.normalize_plate_keyword`（繁体省份字转简体 + 去分隔符 + 大写），入池与匹配两侧归一化规则统一。
3. 移除不再使用的 `import re`。

## 部署步骤

```bash
# 1. 备份并上传
sshpass -p '***' ssh root@43.136.92.192 "cp /home/wxbot/insurance/fill_pending.py /home/wxbot/insurance/fill_pending.py.bak-miss-copy-0721"
sshpass -p '***' scp insurance/fill_pending.py root@43.136.92.192:/home/wxbot/insurance/fill_pending.py

# 2. 修存量繁体车牌（见上方 SQL）

# 3. 平滑重载 gunicorn
sshpass -p '***' ssh root@43.136.92.192 "kill -HUP \$(cat /home/wxbot/gunicorn.pid)"
```

另需线下处理：拿 taskId 向飞流（FlowBot）反馈 @所有人 误执行问题；确认机器人账号在各客户群是否为群主/管理员，无必要则取消。
