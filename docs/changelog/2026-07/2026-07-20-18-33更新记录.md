# 2026-07-20 18:33 更新记录

## 需求说明

手机端台账列表页：时间筛选行（创建日期类型下拉 + 全部/今天/昨天/本周/上周/本月/上月/自定义快捷按钮 + 排序 + 高级搜索按钮）从「筛选」收起面板中拆出，固定显示在页面上，方便用户随时切换时间范围，无需先展开筛选面板。

## 数据库变更

无。

## 代码变更

- `web/templates/insurance.html`
  - 移动端 CSS（`@media (max-width: 767px)`）：`.record-filter-card` 由整体隐藏改为始终显示；新增规则 —— 收起状态下仅隐藏上半部分筛选条件 `.rf-main`（来源/群名/文件名/保单号/车牌/投保人/保司/险种/企业/核对状态），时间筛选行 `.rf-date` 固定可见，并去掉收起时的上边距与分隔线
  - 筛选卡片内两处 div 分别添加 `rf-main`、`rf-date` 类名
  - 桌面端（≥768px）布局与行为不变

## 部署步骤

1. 上传 `web/templates/insurance.html` 至服务器 `/home/wxbot/web/templates/insurance.html`
2. 生成去除 `{% raw %}` / `{% endraw %}` 标记的静态版本，上传至 `/home/wxbot/web/static/insurance.html`（线上 /insurance 由 nginx 直接提供该静态文件）
3. 无需重启服务
