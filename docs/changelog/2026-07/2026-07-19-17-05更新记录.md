# 停用钉钉多维表自动同步

> 2026-07-19 17:05

## 需求说明

钉钉多维表格同步功能已停用（业务确认下线）。核查线上 24 个监控配置均未绑定钉钉文档（dingtalk_base_id 全部为空），自动同步实际早已不触发；本次将代码中的自动同步调用注释掉，彻底停用，避免歧义。

## 数据库变更

无。

## 代码变更

| 文件 | 变更 |
|------|------|
| `insurance/handler.py` | `_process` 第 9 步「识别完成自动同步钉钉多维表」整段注释；`_apply_cross_fill` 与 `_cross_fill_by_person` 反向互补后的 `_resync_record_to_dingtalk` 调用注释 |

保留未删（便于将来恢复）：

- `_sync_to_dingtalk_v2` / `_resync_record_to_dingtalk` 函数本体
- `insurance/dingtalk_sync.py` 模块
- 前端手动重同步 API（`insurance/api.py` 两处，因监控配置未绑定钉钉文档，调用会返回「未绑定」不生效）

注：钉钉**群机器人通知**（系统告警、新保司通知、未录入通知等 webhook 推送）不属于多维表同步，不受影响，照常使用。

## 部署步骤

1. 上传 `insurance/handler.py` 到服务器 `/home/wxbot/insurance/`（gunicorn `--reload` 自动生效）
2. 无需改配置、无需建表
