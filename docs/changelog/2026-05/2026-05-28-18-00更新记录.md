# 2026-05-28 18:00 更新记录

## 需求说明
修复保单识别列表中自定义添加的列无法编辑保存数据的问题。

## 问题原因
`apply_mapping()` 函数在重新计算 `display_fields` 时，只遍历预定义的 `output_columns`（20个标准列），自定义列的值在计算过程中被丢弃。

## 代码变更
- `insurance/field_mapping.py`：`apply_mapping()` 函数末尾增加自定义字段透传逻辑，将不在已知 OCR 字段和 output_columns 中的字段直接保留到 display_fields 中。

## 部署步骤
重启服务即可，无数据库变更。
