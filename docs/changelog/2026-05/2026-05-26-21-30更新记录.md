# 2026-05-26 按车牌合并导出排除无效车牌

## 需求说明
按车牌合并导出时，车牌为空、"新车"、"*-*" 的记录不应参与合并，应作为独立行导出。

## 代码变更
- `insurance/api.py`
  - `export_excel()`: 增加 `_plate_mergeable()` 判断函数，空车牌、"新车"、"*-*"/"*—*" 的记录不参与分组合并，作为独立行导出（列结构与合并行保持一致）

## 部署步骤
重启服务即可，无数据库变更。
