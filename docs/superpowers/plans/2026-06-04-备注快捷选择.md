# 备注快捷选择（联动选择器）实现计划

> **For agentic workers:** 本项目无测试框架（见 CLAUDE.md），按项目既有模式验证：导入自检 / 启动服务 / 手动核对。步骤用 `- [ ]` 跟踪。

**Goal:** 在台账「备注」字段上提供按经办人/投保公司/险种联动筛选的快捷选择器，选项池来自每日上传的 Excel，可清空重导且不影响历史已选值。

**Architecture:** 新建独立选项池表 `insurance_remark_options`（按 scope 隔离，导入=清空+插入）；配置 `remark_selector` 存 `user_field_config`（沿用 custom_fixed_values 模式，按企业/个人 scope）；后端新增 remark-options 系列接口；前端 `insurance.html` 加单元格「选择」入口+选择器弹窗，`field_config.html` 加设置区块。

**Tech Stack:** Flask Blueprint + PooledDB/pymysql + openpyxl + Vue3(CDN) + authFetch。

---

### Task 1: 选项池表自动建表

**Files:**
- Modify: `insurance/db.py`（建表区，参照 `insurance_records` 自动建表/补列模式，约 325-373 行后追加）

- [ ] **Step 1: 在 init/建表流程追加建表 SQL**

在 `insurance/db.py` 建表方法内（与其它 `CREATE TABLE IF NOT EXISTS` / 索引创建同段落）追加：

```python
        # 备注快捷选择 - 选项池表（按 scope 隔离，每日清空重导）
        try:
            cursor.execute(
                """
                CREATE TABLE IF NOT EXISTS insurance_remark_options (
                    id BIGINT PRIMARY KEY AUTO_INCREMENT,
                    scope VARCHAR(16) NOT NULL DEFAULT 'global' COMMENT 'global/enterprise/user',
                    scope_id VARCHAR(64) DEFAULT NULL COMMENT '企业parent_id或user_id',
                    handler VARCHAR(64) DEFAULT '' COMMENT '经办人',
                    company VARCHAR(128) DEFAULT '' COMMENT '投保公司',
                    policy_type VARCHAR(64) DEFAULT '' COMMENT '险种名称',
                    remark TEXT COMMENT '备注信息',
                    batch_no VARCHAR(32) DEFAULT '' COMMENT '导入批次(时间戳)',
                    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
                    KEY idx_remark_opt_scope (scope, scope_id, handler, company, policy_type)
                ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='备注快捷选择选项池'
                """
            )
        except Exception:
            pass
```

- [ ] **Step 2: 导入自检**

Run: `cd /Users/take/Downloads/yun/web/wxbot && python -c "import insurance.db"`
Expected: 无报错（macOS 上 SDK 降级警告可忽略）。

- [ ] **Step 3: Commit**

```bash
git add insurance/db.py
git commit -m "feat: 备注快捷选择-新增选项池表 insurance_remark_options"
```

---

### Task 2: 选项池数据层 `remark_options_db.py`

**Files:**
- Create: `insurance/remark_options_db.py`

- [ ] **Step 1: 新建模块（解析 Excel sheet 列表、导入、查询、清空、统计）**

```python
"""
备注快捷选择 - 选项池数据层。

- read_sheet_names: 读取 Excel 全部 sheet 名（供前端选 sheet）
- parse_sheet_rows: 按表头(经办人/投保公司/备注信息/险种名称)解析指定 sheet 的行
- import_options: 清空当前 scope 旧池 + 批量插入新池（同事务）
- query_options: 按 handler/company/policy_type/keyword + mode(exact/fuzzy) 查询候选 remark
- clear_options / get_stats
"""

import io
import logging
from datetime import datetime

import openpyxl
import pymysql
import pymysql.cursors

logger = logging.getLogger(__name__)

# Excel 表头 → 内部字段
_HEADER_MAP = {
    "经办人": "handler",
    "投保公司": "company",
    "备注信息": "remark",
    "险种名称": "policy_type",
    "险种": "policy_type",
}


def read_sheet_names(file_bytes: bytes) -> list:
    """返回 Excel 全部 sheet 名（顺序与文件一致）。"""
    wb = openpyxl.load_workbook(io.BytesIO(file_bytes), read_only=True, data_only=True)
    try:
        return list(wb.sheetnames)
    finally:
        wb.close()


def parse_sheet_rows(file_bytes: bytes, sheet_name: str) -> list:
    """
    解析指定 sheet，返回 [{"handler","company","policy_type","remark"}, ...]。
    按表头名定位列（容忍尾部多余空列、列顺序变化）；remark 为空的行跳过。
    """
    wb = openpyxl.load_workbook(io.BytesIO(file_bytes), read_only=True, data_only=True)
    try:
        if sheet_name not in wb.sheetnames:
            raise ValueError(f"sheet 不存在: {sheet_name}")
        ws = wb[sheet_name]
        rows_iter = ws.iter_rows(values_only=True)
        try:
            header = next(rows_iter)
        except StopIteration:
            return []
        # 表头列名 → 列索引
        col_idx = {}
        for i, name in enumerate(header):
            key = _HEADER_MAP.get(str(name).strip()) if name is not None else None
            if key and key not in col_idx:
                col_idx[key] = i
        if "remark" not in col_idx:
            raise ValueError("未找到「备注信息」列")

        def _cell(row, key):
            i = col_idx.get(key)
            if i is None or i >= len(row) or row[i] is None:
                return ""
            return str(row[i]).strip()

        out = []
        for row in rows_iter:
            if row is None:
                continue
            remark = _cell(row, "remark")
            if not remark:
                continue
            out.append({
                "handler": _cell(row, "handler"),
                "company": _cell(row, "company"),
                "policy_type": _cell(row, "policy_type"),
                "remark": remark,
            })
        return out
    finally:
        wb.close()


def _scope_cond(scope, scope_id):
    """统一 scope 过滤条件（scope_id 为 None 时用 IS NULL）。"""
    if scope_id is None:
        return "scope = %s AND scope_id IS NULL", [scope]
    return "scope = %s AND scope_id = %s", [scope, str(scope_id)]


def import_options(db, scope, scope_id, rows: list) -> int:
    """清空当前 scope 旧池 + 批量插入新池（去重 handler+company+policy_type+remark）。返回插入条数。"""
    batch_no = datetime.now().strftime("%Y%m%d%H%M%S")
    seen = set()
    dedup = []
    for r in rows:
        k = (r["handler"], r["company"], r["policy_type"], r["remark"])
        if k in seen:
            continue
        seen.add(k)
        dedup.append(r)

    cond, params = _scope_cond(scope, scope_id)
    conn = db.pool.connection()
    try:
        cursor = conn.cursor()
        cursor.execute(f"DELETE FROM insurance_remark_options WHERE {cond}", params)
        if dedup:
            sid = None if scope_id is None else str(scope_id)
            cursor.executemany(
                "INSERT INTO insurance_remark_options "
                "(scope, scope_id, handler, company, policy_type, remark, batch_no) "
                "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                [(scope, sid, r["handler"], r["company"], r["policy_type"], r["remark"], batch_no)
                 for r in dedup]
            )
        conn.commit()
        logger.info("备注选项池导入: scope=%s scope_id=%s 插入=%d batch=%s",
                    scope, scope_id, len(dedup), batch_no)
        return len(dedup)
    finally:
        conn.close()


def query_options(db, scope, scope_id, handler="", company="", policy_type="",
                  keyword="", mode="exact", limit=500) -> list:
    """
    按键查询候选 remark。mode=exact 精确等值；mode=fuzzy 子串包含。
    任一键为空则该键不参与过滤。keyword 始终按 remark 子串过滤。
    返回去重后的 remark 字符串列表（保序）。
    """
    cond, params = _scope_cond(scope, scope_id)
    where = [cond]

    def _add(field, value):
        value = (value or "").strip()
        if not value:
            return
        if mode == "fuzzy":
            where.append(f"{field} LIKE %s")
            params.append(f"%{value}%")
        else:
            where.append(f"{field} = %s")
            params.append(value)

    _add("handler", handler)
    _add("company", company)
    _add("policy_type", policy_type)
    if keyword and keyword.strip():
        where.append("remark LIKE %s")
        params.append(f"%{keyword.strip()}%")

    sql = ("SELECT remark FROM insurance_remark_options WHERE " + " AND ".join(where) +
           " ORDER BY id LIMIT %s")
    params.append(int(limit))
    conn = db.pool.connection()
    try:
        cursor = conn.cursor(pymysql.cursors.DictCursor)
        cursor.execute(sql, params)
        rows = cursor.fetchall()
        seen = set()
        out = []
        for row in rows:
            rm = row["remark"]
            if rm and rm not in seen:
                seen.add(rm)
                out.append(rm)
        return out
    finally:
        conn.close()


def clear_options(db, scope, scope_id) -> int:
    """清空当前 scope 选项池。返回删除条数。"""
    cond, params = _scope_cond(scope, scope_id)
    conn = db.pool.connection()
    try:
        cursor = conn.cursor()
        n = cursor.execute(f"DELETE FROM insurance_remark_options WHERE {cond}", params)
        conn.commit()
        return n
    finally:
        conn.close()


def get_stats(db, scope, scope_id) -> dict:
    """返回 {"count": N, "last_import": "YYYYMMDDHHMMSS" or ""}。"""
    cond, params = _scope_cond(scope, scope_id)
    conn = db.pool.connection()
    try:
        cursor = conn.cursor(pymysql.cursors.DictCursor)
        cursor.execute(
            f"SELECT COUNT(*) AS cnt, MAX(batch_no) AS last_batch "
            f"FROM insurance_remark_options WHERE {cond}", params)
        row = cursor.fetchone() or {}
        return {"count": int(row.get("cnt") or 0), "last_import": row.get("last_batch") or ""}
    finally:
        conn.close()
```

- [ ] **Step 2: 导入自检**

Run: `cd /Users/take/Downloads/yun/web/wxbot && python -c "import insurance.remark_options_db as m; print(m.read_sheet_names(open('6-4台账备注2.xlsx','rb').read())); print(m.parse_sheet_rows(open('6-4台账备注2.xlsx','rb').read(), '6-4')[:3])"`
Expected: 打印出 sheet 名列表，以及 '6-4' 前 3 行解析结果（含 handler/company/policy_type/remark）。

- [ ] **Step 3: Commit**

```bash
git add insurance/remark_options_db.py
git commit -m "feat: 备注快捷选择-选项池数据层(导入/查询/清空/统计)"
```

---

### Task 3: `remark_selector` 配置读写（field_config_db.py）

**Files:**
- Modify: `insurance/field_config_db.py`（在 `save_user_fixed_values` 之后追加，约 1180 行）

- [ ] **Step 1: 追加按 scope 的配置读写**

配置存 `user_field_config`，`config_type='remark_selector'`，`config_key='config'`，scope 由调用方传入（企业/个人/全局）。

```python
# ------------------------------------------------------------------ #
# 备注快捷选择配置（remark_selector），按 scope 存储
# ------------------------------------------------------------------ #

REMARK_SELECTOR_DEFAULT = {
    "enabled": False,
    "target_key": "备注",
    "key_handler": "经办人",
    "key_company": "承保公司",
    "key_policy_type": "险种",
}


def _scope_id_cond_rs(scope_id):
    if scope_id is None:
        return "scope_id IS NULL", []
    return "scope_id = %s", [scope_id]


def get_remark_selector_config(db, scope: str, scope_id) -> dict:
    """读取指定 scope 的备注快捷选择配置；无则返回默认值副本。"""
    sid_cond, sid_params = _scope_id_cond_rs(scope_id)
    conn = db.pool.connection()
    try:
        cursor = conn.cursor(pymysql.cursors.DictCursor)
        cursor.execute(
            f"SELECT config_value FROM user_field_config "
            f"WHERE scope = %s AND {sid_cond} AND config_type = 'remark_selector' "
            f"AND config_key = 'config' LIMIT 1",
            [scope] + sid_params
        )
        row = cursor.fetchone()
        result = dict(REMARK_SELECTOR_DEFAULT)
        if row and row.get("config_value"):
            try:
                result.update(json.loads(row["config_value"]))
            except (TypeError, json.JSONDecodeError):
                pass
        return result
    finally:
        conn.close()


def save_remark_selector_config(db, scope: str, scope_id, config: dict):
    """保存指定 scope 的备注快捷选择配置（仅保留已知键）。"""
    clean = dict(REMARK_SELECTOR_DEFAULT)
    for k in REMARK_SELECTOR_DEFAULT:
        if k in (config or {}):
            clean[k] = config[k]
    clean["enabled"] = bool(clean.get("enabled"))
    json_val = json.dumps(clean, ensure_ascii=False)
    sid_cond, sid_params = _scope_id_cond_rs(scope_id)
    conn = db.pool.connection()
    try:
        cursor = conn.cursor()
        cursor.execute(
            f"SELECT id FROM user_field_config "
            f"WHERE scope = %s AND {sid_cond} AND config_type = 'remark_selector' "
            f"AND config_key = 'config' LIMIT 1",
            [scope] + sid_params
        )
        if cursor.fetchone():
            cursor.execute(
                f"UPDATE user_field_config SET config_value = %s, updated_at = NOW() "
                f"WHERE scope = %s AND {sid_cond} AND config_type = 'remark_selector' "
                f"AND config_key = 'config'",
                [json_val, scope] + sid_params
            )
        else:
            cursor.execute(
                "INSERT INTO user_field_config (scope, scope_id, config_type, config_key, config_value, template_name) "
                "VALUES (%s, %s, 'remark_selector', 'config', %s, %s)",
                [scope, scope_id, json_val, DEFAULT_TEMPLATE_NAME]
            )
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 2: 注册 config_type**

修改 `CONFIG_TYPES`（约 20 行）追加 `"remark_selector"`：

```python
CONFIG_TYPES = ["company_alias", "policy_type_alias", "date_format", "fee_formula",
                "list_columns", "export_columns", "remark_selector"]
```

- [ ] **Step 3: 导入自检**

Run: `cd /Users/take/Downloads/yun/web/wxbot && python -c "import insurance.field_config_db as f; print(f.REMARK_SELECTOR_DEFAULT)"`
Expected: 打印默认配置 dict。

- [ ] **Step 4: Commit**

```bash
git add insurance/field_config_db.py
git commit -m "feat: 备注快捷选择-remark_selector 配置读写(按scope)"
```

---

### Task 4: 后端接口（api.py）

**Files:**
- Modify: `insurance/api.py`（import 区 + 在 `/api/insurance/config` 路由附近追加新路由）

- [ ] **Step 1: 顶部导入新模块与配置函数**

在 api.py 现有 `from insurance.field_config_db import ...` 处追加 `get_remark_selector_config, save_remark_selector_config`；并新增：

```python
from insurance import remark_options_db
```

- [ ] **Step 2: 追加 remark-options 接口（放在 /api/insurance/config PUT 之后，约 2165 行后）**

```python
# ============================================================
# 备注快捷选择（选项池 + 配置）
# ============================================================

@insurance_bp.route("/api/insurance/remark-options/config", methods=["GET"])
@login_required
def get_remark_selector_config_api():
    """读取当前用户 scope 的备注快捷选择配置。"""
    try:
        scope, scope_id = _get_user_scope()
        cfg = get_remark_selector_config(_db, scope, scope_id)
        stats = remark_options_db.get_stats(_db, scope, scope_id)
        return jsonify({"code": 0, "data": {"config": cfg, "stats": stats}})
    except Exception as e:
        logger.exception("读取备注快捷选择配置失败")
        return jsonify({"code": 500, "msg": str(e)}), 500


@insurance_bp.route("/api/insurance/remark-options/config", methods=["PUT"])
@login_required
def save_remark_selector_config_api():
    """保存当前用户 scope 的备注快捷选择配置。"""
    try:
        body = request.get_json(force=True) or {}
        scope, scope_id = _get_user_scope()
        save_remark_selector_config(_db, scope, scope_id, body.get("config", body))
        return jsonify({"code": 0, "msg": "已保存"})
    except Exception as e:
        logger.exception("保存备注快捷选择配置失败")
        return jsonify({"code": 500, "msg": str(e)}), 500


@insurance_bp.route("/api/insurance/remark-options/sheets", methods=["POST"])
@login_required
def remark_options_sheets():
    """上传 Excel，返回 sheet 名列表（不落库）。"""
    try:
        f = request.files.get("file")
        if not f:
            return jsonify({"code": 400, "msg": "缺少文件"}), 400
        names = remark_options_db.read_sheet_names(f.read())
        return jsonify({"code": 0, "data": {"sheets": names}})
    except Exception as e:
        logger.exception("读取 sheet 列表失败")
        return jsonify({"code": 500, "msg": str(e)}), 500


@insurance_bp.route("/api/insurance/remark-options/import", methods=["POST"])
@login_required
def remark_options_import():
    """上传 Excel + 选定 sheet → 清空当前 scope 旧池 + 导入新池。"""
    try:
        f = request.files.get("file")
        sheet = request.form.get("sheet", "")
        if not f or not sheet:
            return jsonify({"code": 400, "msg": "缺少文件或 sheet"}), 400
        rows = remark_options_db.parse_sheet_rows(f.read(), sheet)
        scope, scope_id = _get_user_scope()
        n = remark_options_db.import_options(_db, scope, scope_id, rows)
        return jsonify({"code": 0, "msg": f"导入成功 {n} 条", "data": {"count": n}})
    except Exception as e:
        logger.exception("导入备注选项池失败")
        return jsonify({"code": 500, "msg": str(e)}), 500


@insurance_bp.route("/api/insurance/remark-options/query", methods=["GET"])
@login_required
def remark_options_query():
    """按 handler/company/policy_type/keyword + mode 查询候选 remark。"""
    try:
        scope, scope_id = _get_user_scope()
        opts = remark_options_db.query_options(
            _db, scope, scope_id,
            handler=request.args.get("handler", ""),
            company=request.args.get("company", ""),
            policy_type=request.args.get("policy_type", ""),
            keyword=request.args.get("keyword", ""),
            mode=request.args.get("mode", "exact"),
        )
        return jsonify({"code": 0, "data": {"options": opts}})
    except Exception as e:
        logger.exception("查询备注候选失败")
        return jsonify({"code": 500, "msg": str(e)}), 500


@insurance_bp.route("/api/insurance/remark-options", methods=["DELETE"])
@login_required
def remark_options_clear():
    """清空当前 scope 选项池。"""
    try:
        scope, scope_id = _get_user_scope()
        n = remark_options_db.clear_options(_db, scope, scope_id)
        return jsonify({"code": 0, "msg": f"已清空 {n} 条"})
    except Exception as e:
        logger.exception("清空备注选项池失败")
        return jsonify({"code": 500, "msg": str(e)}), 500
```

- [ ] **Step 3: 导入自检**

Run: `cd /Users/take/Downloads/yun/web/wxbot && python -c "import insurance.api"`
Expected: 无报错。

- [ ] **Step 4: Commit**

```bash
git add insurance/api.py
git commit -m "feat: 备注快捷选择-后端接口(sheets/import/query/clear/config)"
```

---

### Task 5: 列表选择器入口 + 弹窗（insurance.html）

**Files:**
- Modify: `web/templates/insurance.html`（单元格渲染区 ~1287-1298；Vue setup data/methods；新增弹窗 DOM）

- [ ] **Step 1: 单元格追加「选择」按钮**

在 `insurance.html` 备注单元格（约 1296 行「修改/填写」span 之后，仍在外层 `<span @click=startListEdit>` 内或紧邻）追加，仅当该列是备注目标列且功能开启时显示。放在外层可点击 span **之外**避免触发编辑：

```html
<button v-if="remarkSelector.enabled && col.key === remarkSelector.config.target_key"
        @click.stop="openRemarkSelector(record)"
        class="text-xs rounded px-1.5 py-0.5 cursor-pointer"
        style="background:#fff7ed;border:1px solid #fdba74;color:#c2410c;margin-left:6px;"
        title="按经办人/投保公司/险种筛选备注">选择</button>
```

（注意：此按钮需移到 `startListEdit` 的可点击 span 外层同级，避免点击冒泡触发编辑；实现时把按钮放在该 `<td>` 内、编辑 span 之后。）

- [ ] **Step 2: Vue data 中新增状态（在 setup() 内、与 listEditing 同区）**

```javascript
const remarkSelector = reactive({
    enabled: false,
    config: { target_key: '备注', key_handler: '经办人', key_company: '承保公司', key_policy_type: '险种' },
});
const remarkPicker = reactive({
    show: false, record: null, loading: false,
    mode: 'exact',           // exact | fuzzy
    handler: '', company: '', policy_type: '',   // 当前筛选键（可临时清空放宽）
    useHandler: true, useCompany: true, usePolicyType: true,
    keyword: '', options: [],
});
```

- [ ] **Step 3: methods — 加载配置 / 打开弹窗 / 查询 / 选取**

```javascript
async function loadRemarkSelectorConfig() {
    try {
        const resp = await authFetch('/api/insurance/remark-options/config');
        const json = await resp.json();
        if (json.code === 0 && json.data) {
            remarkSelector.config = json.data.config;
            remarkSelector.enabled = !!json.data.config.enabled;
        }
    } catch (e) { /* 静默：未配置不影响列表 */ }
}

function openRemarkSelector(record) {
    const df = record.display_fields || {};
    const c = remarkSelector.config;
    remarkPicker.record = record;
    remarkPicker.handler = df[c.key_handler] || '';
    remarkPicker.company = df[c.key_company] || '';
    remarkPicker.policy_type = df[c.key_policy_type] || '';
    remarkPicker.useHandler = !!remarkPicker.handler;
    remarkPicker.useCompany = !!remarkPicker.company;
    remarkPicker.usePolicyType = !!remarkPicker.policy_type;
    remarkPicker.mode = 'exact';
    remarkPicker.keyword = '';
    remarkPicker.show = true;
    queryRemarkOptions();
}

async function queryRemarkOptions() {
    remarkPicker.loading = true;
    try {
        const p = new URLSearchParams();
        if (remarkPicker.useHandler) p.set('handler', remarkPicker.handler);
        if (remarkPicker.useCompany) p.set('company', remarkPicker.company);
        if (remarkPicker.usePolicyType) p.set('policy_type', remarkPicker.policy_type);
        if (remarkPicker.keyword) p.set('keyword', remarkPicker.keyword);
        p.set('mode', remarkPicker.mode);
        const resp = await authFetch('/api/insurance/remark-options/query?' + p.toString());
        const json = await resp.json();
        remarkPicker.options = (json.code === 0 && json.data) ? json.data.options : [];
    } catch (e) {
        remarkPicker.options = [];
    } finally {
        remarkPicker.loading = false;
    }
}

async function pickRemark(value) {
    const record = remarkPicker.record;
    const col = remarkSelector.config.target_key;
    // 复用单元格保存逻辑：进入编辑态→写值→保存（确保计入 manual_fields）
    listEditing.recordId = record.id;
    listEditing.col = col;
    listEditing.value = value;
    listEditing.saving = false;
    await saveListEdit(record);
    remarkPicker.show = false;
}
```

- [ ] **Step 4: 在 onMounted 调用 loadRemarkSelectorConfig，并 return 暴露新符号**

在 setup 的 `onMounted(...)` 内追加 `loadRemarkSelectorConfig();`，并在 `return { ... }` 中加入 `remarkSelector, remarkPicker, openRemarkSelector, queryRemarkOptions, pickRemark`。

- [ ] **Step 5: 弹窗 DOM（在根容器末尾、其它 modal 同级追加）**

```html
<!-- 备注快捷选择弹窗 -->
<div v-if="remarkPicker.show" class="fixed inset-0 z-50 flex items-center justify-center"
     style="background:rgba(0,0,0,.4);" @click.self="remarkPicker.show=false">
  <div class="bg-white rounded-lg shadow-xl w-full max-w-2xl mx-4" style="max-height:80vh;display:flex;flex-direction:column;">
    <div class="px-5 py-3 border-b flex items-center justify-between">
      <h3 class="font-semibold text-gray-800">选择备注</h3>
      <button @click="remarkPicker.show=false" class="text-gray-400 hover:text-gray-600">✕</button>
    </div>
    <div class="px-5 py-3 border-b space-y-2 text-sm">
      <div class="flex flex-wrap items-center gap-3">
        <label class="flex items-center gap-1"><input type="checkbox" v-model="remarkPicker.useHandler" @change="queryRemarkOptions">经办人：{{ remarkPicker.handler || '—' }}</label>
        <label class="flex items-center gap-1"><input type="checkbox" v-model="remarkPicker.useCompany" @change="queryRemarkOptions">投保公司：{{ remarkPicker.company || '—' }}</label>
        <label class="flex items-center gap-1"><input type="checkbox" v-model="remarkPicker.usePolicyType" @change="queryRemarkOptions">险种：{{ remarkPicker.policy_type || '—' }}</label>
      </div>
      <div class="flex items-center gap-3">
        <label class="flex items-center gap-1"><input type="radio" value="exact" v-model="remarkPicker.mode" @change="queryRemarkOptions">精准</label>
        <label class="flex items-center gap-1"><input type="radio" value="fuzzy" v-model="remarkPicker.mode" @change="queryRemarkOptions">模糊</label>
        <input v-model="remarkPicker.keyword" @input="queryRemarkOptions" placeholder="搜索备注关键词"
               class="flex-1 border rounded px-2 py-1">
      </div>
    </div>
    <div class="px-5 py-2 overflow-auto" style="flex:1;">
      <div v-if="remarkPicker.loading" class="text-gray-400 text-sm py-4 text-center">加载中…</div>
      <div v-else-if="!remarkPicker.options.length" class="text-gray-400 text-sm py-4 text-center">未匹配到，可放宽筛选或直接手动填写</div>
      <ul v-else class="divide-y">
        <li v-for="(opt,idx) in remarkPicker.options" :key="idx"
            @click="pickRemark(opt)"
            class="py-2 px-2 cursor-pointer hover:bg-orange-50 text-sm text-gray-700">{{ opt }}</li>
      </ul>
    </div>
  </div>
</div>
```

- [ ] **Step 6: 手动验证**

启动服务，登录后打开 `/insurance`：在设置开启功能并导入选项池后，备注单元格出现「选择」按钮，点击弹窗按当前行经办人/投保公司/险种筛出候选；勾选/取消键、切换精准/模糊、搜索均实时刷新；点选写回单元格并保存成功。

- [ ] **Step 7: Commit**

```bash
git add web/templates/insurance.html
git commit -m "feat: 备注快捷选择-列表选择器入口与弹窗"
```

---

### Task 6: 设置页区块（field_config.html）

**Files:**
- Modify: `web/templates/field_config.html`（新增「备注快捷选择」设置卡片 + Vue 状态/方法）

- [ ] **Step 1: 新增设置卡片 DOM（与其它设置区块同级）**

```html
<div class="setting-card">
  <h3>备注快捷选择</h3>
  <label class="flex items-center gap-2 mb-2">
    <input type="checkbox" v-model="remarkCfg.enabled"> 启用备注快捷选择
  </label>
  <div class="grid grid-cols-2 gap-2 mb-3 text-sm">
    <label>备注目标字段
      <select v-model="remarkCfg.target_key" class="border rounded px-2 py-1 w-full">
        <option v-for="k in remarkFieldKeys" :key="k" :value="k">{{ k }}</option>
      </select></label>
    <label>经办人字段
      <select v-model="remarkCfg.key_handler" class="border rounded px-2 py-1 w-full">
        <option v-for="k in remarkFieldKeys" :key="k" :value="k">{{ k }}</option>
      </select></label>
    <label>投保公司字段
      <select v-model="remarkCfg.key_company" class="border rounded px-2 py-1 w-full">
        <option v-for="k in remarkFieldKeys" :key="k" :value="k">{{ k }}</option>
      </select></label>
    <label>险种字段
      <select v-model="remarkCfg.key_policy_type" class="border rounded px-2 py-1 w-full">
        <option v-for="k in remarkFieldKeys" :key="k" :value="k">{{ k }}</option>
      </select></label>
  </div>
  <button @click="saveRemarkCfg" class="btn-primary">保存配置</button>
  <hr class="my-3">
  <div class="text-sm mb-2">当前选项池：{{ remarkStats.count }} 条
    <span v-if="remarkStats.last_import">· 最后导入 {{ remarkStats.last_import }}</span></div>
  <input type="file" accept=".xlsx,.xls" @change="onRemarkFile" ref="remarkFileEl">
  <select v-if="remarkSheets.length" v-model="remarkSheet" class="border rounded px-2 py-1 ml-2">
    <option v-for="s in remarkSheets" :key="s" :value="s">{{ s }}</option>
  </select>
  <button v-if="remarkSheets.length" @click="doRemarkImport" class="btn-primary ml-2">导入选定 sheet</button>
  <button @click="clearRemarkPool" class="btn-secondary ml-2">清空选项池</button>
</div>
```

- [ ] **Step 2: Vue 状态（setup 内）**

```javascript
const remarkCfg = reactive({ enabled:false, target_key:'备注', key_handler:'经办人', key_company:'承保公司', key_policy_type:'险种' });
const remarkStats = reactive({ count:0, last_import:'' });
const remarkFieldKeys = ref([]);   // 可选字段：取自当前列配置
const remarkSheets = ref([]);
const remarkSheet = ref('');
let remarkFile = null;
```

- [ ] **Step 3: methods**

```javascript
async function loadRemarkCfg() {
    const resp = await authFetch('/api/insurance/remark-options/config');
    const json = await resp.json();
    if (json.code === 0) { Object.assign(remarkCfg, json.data.config); Object.assign(remarkStats, json.data.stats); }
    // 字段候选：复用已加载的列配置 keys（变量名按本文件实际列配置数组调整）
    try {
        const cresp = await authFetch('/api/insurance/column-config?config_type=list_columns');
        const cjson = await cresp.json();
        remarkFieldKeys.value = (cjson.data && cjson.data.columns ? cjson.data.columns : []).map(c => c.key);
    } catch (e) { remarkFieldKeys.value = []; }
}
async function saveRemarkCfg() {
    await authFetch('/api/insurance/remark-options/config', { method:'PUT', body: JSON.stringify({ config: remarkCfg }) });
    alert('已保存');
}
function onRemarkFile(e) {
    remarkFile = e.target.files[0]; remarkSheets.value = []; remarkSheet.value = '';
    if (!remarkFile) return;
    const fd = new FormData(); fd.append('file', remarkFile);
    authFetch('/api/insurance/remark-options/sheets', { method:'POST', body: fd })
      .then(r=>r.json()).then(j=>{ if(j.code===0){ remarkSheets.value=j.data.sheets; remarkSheet.value=j.data.sheets[j.data.sheets.length-1]||''; }});
}
async function doRemarkImport() {
    if (!remarkFile || !remarkSheet.value) return;
    const fd = new FormData(); fd.append('file', remarkFile); fd.append('sheet', remarkSheet.value);
    const r = await authFetch('/api/insurance/remark-options/import', { method:'POST', body: fd });
    const j = await r.json(); alert(j.msg || '完成'); loadRemarkCfg();
}
async function clearRemarkPool() {
    if (!confirm('确认清空当前选项池？历史已选备注不受影响')) return;
    const r = await authFetch('/api/insurance/remark-options', { method:'DELETE' });
    const j = await r.json(); alert(j.msg || '已清空'); loadRemarkCfg();
}
```

- [ ] **Step 4: onMounted 调 loadRemarkCfg，return 暴露上述符号**

在本文件 setup 的 `onMounted` 追加 `loadRemarkCfg();`，并在 `return {}` 暴露 `remarkCfg, remarkStats, remarkFieldKeys, remarkSheets, remarkSheet, onRemarkFile, doRemarkImport, saveRemarkCfg, clearRemarkPool`。

- [ ] **Step 5: 手动验证**

启动服务 → `/insurance/settings`：开启功能、配置四个字段映射、上传 `6-4台账备注2.xlsx`、选 sheet（默认最后一个）导入，统计条数更新；清空按钮可清空。

- [ ] **Step 6: Commit**

```bash
git add web/templates/field_config.html
git commit -m "feat: 备注快捷选择-设置页区块(配置/上传导入/清空)"
```

---

### Task 7: 更新记录文档

**Files:**
- Create: `docs/changelog/2026-06/2026-06-04-HH-mm更新记录.md`

- [ ] **Step 1: 写更新记录**

包含：需求说明、数据库变更（新表 `insurance_remark_options`；新 config_type `remark_selector`）、代码变更（5 个文件）、部署步骤（重启服务自动建表；设置页配置字段映射并导入 Excel）。

- [ ] **Step 2: Commit & push**

```bash
git add docs/changelog/2026-06/
git commit -m "docs: 备注快捷选择更新记录"
git push
```

---

## 自检（Self-Review）

- **Spec 覆盖**：选项池表(Task1)、数据层(Task2)、配置(Task3)、6 个接口(Task4)、列表入口+弹窗(Task5)、设置页(Task6) 全覆盖。多 sheet 默认最后一个（Task6 Step3）、精准默认/模糊切换（Task2 query + Task5 弹窗）、按企业/个人 scope（_get_user_scope）、不影响历史已选（pickRemark 走 saveListEdit→manual_fields，选项池与记录解耦）均落实。
- **类型一致**：内部字段统一 handler/company/policy_type/remark；接口参数 handler/company/policy_type/keyword/mode 在 Task4 与 Task5 一致；配置键 enabled/target_key/key_handler/key_company/key_policy_type 在 Task3/5/6 一致。
- **占位符**：无 TBD。Task5 Step1、Task6 Step3 注明需按 insurance.html / field_config.html 实际列配置变量名微调（这些是真实文件内既有结构，执行时读取确认）。

## 待确认默认值（不阻塞实现，影响默认填充准确性）

- 投保公司在记录里的真实字段名（默认 `承保公司`）—— 设置页可由用户改。
- 是否需要模糊匹配公司分支名 —— 已内置「精准/模糊」切换，默认精准。
